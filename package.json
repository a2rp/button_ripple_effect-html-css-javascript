{
  "name": "button-ripple-effect",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx --yes serve .",
    "build": "node build.mjs",
    "deploy": "npm run build && npx --yes gh-pages -d dist -b gh-pages"
  }
}
